package com.parent.portal.user_management_service.entity;

public enum Roles {
    PARENT,
    ADMIN

    // You can add methods here if you need more complex logic for roles,
    // for example, to get a display name or check permissions.
    // public String getDisplayName() {
    //     return this.name().charAt(0) + this.name().substring(1).toLowerCase();
    // }
}
